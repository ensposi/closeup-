import { StyleSheet } from 'react-native';

export const colors = { bg: '#FFFFFF', text: '#111111', muted: '#6B7280', border: '#E5E7EB', accent: '#111111', card: '#F7F7F7' };
export const spacing = StyleSheet.create({ screen: { padding: 18 }, card: { padding: 16, borderRadius: 16 }, gap: { marginBottom: 12 } });
