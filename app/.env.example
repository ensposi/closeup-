# Supabase public client configuration
# Never commit service-role keys or other private secrets.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Optional map provider configuration, depending on the native build setup.
EXPO_PUBLIC_MAPBOX_ACCESS_TOKEN=
